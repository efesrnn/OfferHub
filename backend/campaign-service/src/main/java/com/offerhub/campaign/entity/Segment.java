package com.offerhub.campaign.entity;

/** Subscriber segments. BELIRSIZ is the fallback when AI is unreachable. */
public enum Segment {
    YUKSEK_DEGER,
    RISKLI_KAYIP,
    YENI_ABONE,
    PASIF,
    BELIRSIZ
}
